package com.example;

public class Main {
  public static void main(String[] args) {
    /**
     *
     * Create a `Student` class with name and GPA.
     * Store a list of students, then sort and display the top 3 using
     * `Collections.sort()` and stream-based sorted().
     */
  }
}
